#!/bin/sh
# Custom settings applied on first boot
# This script auto-deletes after execution

# Set LAN IP to 192.168.10.1 (upstream network is 192.168.1.x — avoid conflict)
uci set network.lan.ipaddr="192.168.10.1"
uci set network.lan.netmask="255.255.255.0"
uci commit network

exit 0
